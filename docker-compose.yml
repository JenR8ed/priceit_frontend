version: '3'

services:
  price_it_frontend:
    container_name: price_it_frontend
    build: .
    env_file: ./.env
    restart: unless-stopped
    ports:
      - ${DOCKER_SERVER_PORT}:${LOCAL_SERVER_PORT}
    volumes:
      - .:/src
      - /src/node_modules
